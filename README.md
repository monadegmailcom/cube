# Try out webgl with three.js 

## Initial Setup

- run `./setup.sh` to setup the project, needs `npm` to be installed on your system
- point your browser to dist/index.html

## (Re-)Build project

- build project `npm run build`
